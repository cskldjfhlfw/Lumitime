# Lumitime frontend environment
#
# Backend integration should keep demo auth disabled so API failures are visible.
# Set to true only for isolated UI demos without a running backend.
VITE_ENABLE_DEMO_AUTH=false
